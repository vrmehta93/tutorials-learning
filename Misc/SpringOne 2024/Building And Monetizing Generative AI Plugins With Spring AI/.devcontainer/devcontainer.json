// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/java
{
	"name": "Java",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/java:1-21-bullseye",

	"features": {
		"ghcr.io/devcontainers/features/java:1": {
			"version": "none",
			"installMaven": "true",
			"installGradle": "false"
		},
		"ghcr.io/devcontainers/features/docker-in-docker:2": {}
	},
	

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [3000, 5432, 8080],

	// Use 'postCreateCommand' to run commands after the container is created.
	// https://code.visualstudio.com/remote/advancedcontainers/start-processes
	// "postCreateCommand": "sudo apt update && sudo apt install -y nodejs && sudo apt install -y npm && mvn clean install -f openai-gpt-plugin-pgvector/backend",
	"postCreateCommand": "./.devcontainer/startupscript.sh",

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "root",	// To run startup script

	"remoteEnv": {
		"OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}",
	},

}
